** 模版


        q.push(head);
        while(!q.empty())
        {
          temp=q.front();
          q.pop();
          if(tempÎ为目标状态)
            输出或记录
          if(temp不合法 )
            continue;
          if(temp合法)
            q.push(temp+¦Δ ); 
